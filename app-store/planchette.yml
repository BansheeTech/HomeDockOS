---
services:
  planchette:
    image: bansheetech/planchette:latest
    container_name: planchette
    ports:
      - "7777:7777"
    volumes:
      - [[INSTALL_PATH]]/planchette:/app
    restart: unless-stopped
